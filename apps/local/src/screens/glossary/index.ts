// natally — glossary-callout module (U.7). Not a route: the frozen route set
// (U.1 router) has no glossary path — the callout mounts over its caller
// (the overAtlas frame), opened by glyphId. I.1 wires the askNatally seam.

export { GlossaryScreen, type GlossaryScreenProps } from "./GlossaryScreen";
