// natally — natal plate module (U.3). Plates are laid inside the transcript,
// not routed: no registerRoute here (the only Atlas route lives in
// screens/atlas). The conversation imports the screen from this barrel.

export {
  NatalPlateScreen,
  type NatalPlateScreenProps,
  natalPlateProvenance,
} from "./natal-plate-screen";
