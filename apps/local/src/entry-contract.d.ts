import type { ComponentType } from "react";

/** T0.2 standalone type boundary; production resolves U.1's actual shell module. */
declare const NatallyShell: ComponentType;
export default NatallyShell;
