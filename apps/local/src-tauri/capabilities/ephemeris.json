{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ephemeris",
  "description": "Local ephemeris access for the main application window.",
  "windows": ["main"],
  "permissions": ["ephemeris:allow-request"]
}
