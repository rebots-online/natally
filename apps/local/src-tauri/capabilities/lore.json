{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lore",
  "description": "Local application storage for the main window.",
  "windows": ["main"],
  "permissions": ["lore:allow-storage"]
}
