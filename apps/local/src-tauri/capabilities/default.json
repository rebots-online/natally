{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core Tauri permissions for the local application window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
