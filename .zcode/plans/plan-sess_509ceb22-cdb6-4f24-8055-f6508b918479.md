# Merge msi4090's sync branch into natally master (this machine) — executes on operator's ping

## Trigger
Operator reports the other ZCode pushed `sync/msi4090-d10-arch-paywall` (its step-7 report: branch, base `7443c66`, HEAD, diffstat). Nothing runs until then.

## Observed facts this plan incorporates
- msi4090 base = `7443c66` (one behind `fa49c31`); the only expected text conflict is CLAUDE.md (their gate-3 append on the old file vs my v1.2.08315 rewrite).
- New-to-master files on their side: `DOCS/ARCHITECTURE.md`, `CHECKLIST.md`, new Figma renders/ledger updates; `DOCS/DECISIONS.md` carries append-only D10–D14.
- Remote layouts differ: msi4090 `origin`=forgejo(down)/`github`=GitHub; this machine `origin`=GitHub. Here I use `origin` directly.

## Steps (this machine, natally repo)
1. **Survey** (read-only): `git fetch origin`; verify branch + base; `git log --oneline master..sync/msi4090-d10-arch-paywall`; `git diff --stat master...sync/...`.
2. **Governance spot-check before merging** (read-only): read D10–D14 appends, CHECKLIST.md protocol header, ARCHITECTURE.md headers; confirm paywall/checkout frames carry operator clearance per D3 — report anything questionable instead of merging silently.
3. **Merge `--no-ff`** with file-level strategy:
   - Theirs: new files (ARCHITECTURE.md, CHECKLIST.md, renders, ledger, .env.example changes), DECISIONS.md appends (clean, append-only).
   - Mine + fold: CLAUDE.md — keep the v1.2 rewrite; fold their gate-3 doctrine bullet (executor scoped to one task block; checklist = dispatch ledger; cross-task need = `[/]` blocked + report; coordination closed at architecting time) into Phase gate 3. Same treatment for README.md if both sides touched it.
   - Version: re-stamp ONCE post-merge (`scripts/update-version.sh` → v1.3.x) — one stamp per handback (INC-18).
4. **Verify**: no tool droppings committed (.zcode/, .v2c/, .video_agent/, STAGING_*); marker/ledger sanity on new files; `git status` clean.
5. **Commit + push** (TC10/CC9): merge commit `v<stamp>: merge msi4090 sync branch — D10–D14, paywall frames, CHECKLIST protocol, ARCHITECTURE stage; CLAUDE.md gate-3 folded into v1.2 structure`; push to origin (GitHub, GIT_LFS_SKIP_PUSH=1 if LFS warns).
6. **Registries (INC-17)**: update natally's Status lines in `~/Admin-Manual/PROJECTS/APP_INVENTORY.md` + `DOCS/PORTFOLIO.md` (Phase advanced: D10–D14, ARCHITECTURE.md exists, paywall scope); scoped commits, push. Note the two-machine remote-layout divergence in the inventory entry.
7. **Report**: delta sc:analyze summary + the resolved CLAUDE.md gate-3 wording.

## Boundaries
No force-push; conflicting doctrine (not just text) → stop and surface. msi4090's checkout stays on its sync branch (switching it back is an operator step there after this merge pushes). No Admin-Manual doctrine edits, no Figma work, no src/ work beyond what the branch contains.