Architect-only mode: stop coding and write the exhaustive spec layer first.

## Phase 1 — DOCS/ARCHITECTURE.md v3 (the exhaustive rewrite)

Rewrite/extend to consume ALL decisions from the strategy doc §12-22 + conversation-established decisions. No placeholders; every enumeration is a closed set.

### 1. Model sourcing — public HF download (replaces §7.1/§13/§16.3/§18.4/§18.7-1)
- Source repos: `unsloth/Qwen3.5-2B-GGUF` (LLM), `onnx-community/Kokoro-82M-v1.0-ONNX` (voice)
- Manifest with absolute `file` URLs + sha256 pins baked at build time (no external manifest host)
- Remove ALL references to `RobinsAIWorld/natally-models`, HF write tokens, HF_TOKEN, mirror publish
- Update .env.example (remove HF_TOKEN, fix VITE_TRIAL_MODEL to qwen3.5-2b)

### 2. New §19 — Shared storage / reusable asset contract (strategy §13-15)
- VITE_STORAGE_SCOPE build-time config + generator script + Vite/Cargo consumption
- Content-identity store (SHA-256 + bytes, object paths)
- Resolve-existing-first resolver + SharedAssets interface + Lookup union + Lease
- Platform adapters: Windows Profile-root, Linux XDG, Android SAF/BlobStore, browser OPFS/Cache Storage
- Concurrency rules, cross-app locking, atomic publish, bundle readiness
- Integration mapping onto existing MirrorStorage/MirrorDownloader/CatalogueStore/loadRuntimeConfig
- Manifest schema amendment: absolute URLs, ZIM/public-lore kinds, dependency graphs, access locators

### 3. New §20 — Windows MSI/MSIX packaging (strategy §19)
- Three distribution channels (Store MSIX / direct MSI / Store-listed installer)
- Partner Center identity contract, AppxManifest template + generator
- MSI/MSIX version ordinal mapping (field limits, monotonicity)
- MakeAppx pack/bundle pipeline, signing, release manifest recording
- Upgrade/acceptance matrix, WebView2 handling
- Fix R.2's overclaimed done-marker (build-windows.sh collects only exe/setup, not msi/msix)

### 4. New §21 — Customer offers + $ROCHE + customer language (strategy §20-21)
- Three-offer table with exact approved copy ("Unlimited chats with natally" / "Pay as you chat" / "Monthly chat credits")
- Catalog separation (lifetime non-consumable / $ROCHE consumables / finite subscription)
- $ROCHE authority split (RC = balances, app service = jobs/reservations/reconciliation)
- Quote/reserve/settle state machine with all states and transitions
- Fungibility matrix across platforms
- Grant/refund lifecycle rules
- Customer language law: no tech terms in customer copy; exact approved micro-copy strings
- Account identity (ecosystem account → RC App User ID, backend-resolved)
- Reconcile §9.6 billing.consume with $ROCHE (not just x402)

### 5. §5 entity table — add rows for all new entities
Storage scope, content identity, catalogue alias, access locator, lease, usage claim, Windows release ordinal, offer catalog entry, service rate card, reservation/job record, grant provenance

### 6. Fix all stale references
- .env.example: remove HF_TOKEN lines, fix VITE_MODEL_MIRROR_BASE, fix VITE_TRIAL_MODEL
- Workflow v2 doc: fix "HF token live" header and "Mirror publish via verified HF token"
- DECISIONS.md: add D-entry for the public-download decision + storage scope + offers + ROCHE (operator decisions from strategy §12-22)

## Phase 2 — CHECKLIST.md v3 (regenerate de novo per D18)
- Preserve v2 predecessor (.bak)
- Derive every task from v3 architecture sections
- Each task: Owns / Spec (architecture-anchored) / Verify / Accept
- Include all new work: storage config, platform adapters, Windows packaging, offers/ROCHE, customer language enforcement
- Fix M.1's stale HF-publish reference
- Census target: every [ ] traceable to a v3 architecture section

## Phase 3 — Commit specs, then return to coding
- Commit ARCHITECTURE v3 + CHECKLIST v3 + .env.example + DECISIONS amendments
- Push to github
- Resume checklist execution from the top of the remaining [ ] wave

No code changes during Phase 1-2 (architect-only mode).