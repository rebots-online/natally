{
  "name": "@natally/lore",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "natally — lore & conversation contracts (ARCHITECTURE §5 entities, §8 Lore subsystem): pure types + zod schemas + the LoreStore interface.",
  "exports": {
    "./types": "./src/types.ts",
    "./store": "./src/store.ts",
    "./ddl": "./src/ddl.ts",
    "./migrate": "./src/migrate.ts",
    "./embed": "./src/embed/embedder.ts",
    "./store/web": "./src/store/web.ts",
    "./store/native": "./src/store/native.ts",
    "./store/common": "./src/store/common.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src tests",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7",
    "better-sqlite3": "^12",
    "typescript": "^5.9",
    "vitest": "^3"
  }
}
